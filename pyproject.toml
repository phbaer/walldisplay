[project]
name = "walldisplay"
version = "1.1.0"
description = "ESP32 wall panel firmware and Home Assistant synchronization tools"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "homeassistant>=2026.2.3",
    "paho-mqtt>=2.1.0",
    "pytest>=9.1.1",
    "voluptuous-serialize>=2.7.0",
]
